"""

A simple Multi Variable Linear Regression Model to estimate the cost of an apartment in Brooklyn.

Data used was manually created in the arrays from listing on Apartments.com on July 3rd, 2023

Author: Isaac Lock
"""

import numpy as np
import matplotlib.pyplot as plt



#Training Data
class apartment:
    pass









#Entry Outline: (Size(sqft), num_bedrooms, num_bathrooms, )
features = np.array([])
targets = np.array([])
